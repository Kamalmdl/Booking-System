# Copy this file to .env and fill in real values.
# .env is git-ignored and must never be committed.

# PostgreSQL password used by all databases (docker-compose) and services
DB_PASSWORD=change_me


